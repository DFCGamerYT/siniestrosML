image:
  repository: mrraiikiri/siniestros-ml
  tag: 65
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8000

config:
  APP_NAME: "Siniestros ML API"
  APP_VERSION: "1.0.0"
  DEBUG: false
  HOST: "0.0.0.0"
  PORT: 8000
  ML_MODEL_PATH: "models/"
  PREDICTION_CONFIDENCE_THRESHOLD: 0.7
